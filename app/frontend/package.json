{
  "name": "task-manager-frontend",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "echo 'Aici ar rula comanda de build reala (ex: vite build)' && mkdir -p dist && echo '<h1>Task Manager</h1><p>Frontend-ul ruleaza!</p>' > dist/index.html"
  }
}
